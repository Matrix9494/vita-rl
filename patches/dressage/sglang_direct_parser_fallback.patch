diff --git a/dressage/proxy/sglang_client.py b/dressage/proxy/sglang_client.py
index 0000000..0000000 100644
--- a/dressage/proxy/sglang_client.py
+++ b/dressage/proxy/sglang_client.py
@@ -563,9 +563,13 @@
             return None
 
         try:
-            workers = await self.list_workers()
+            workers = list(await self.list_workers())
         except Exception:
-            return None
+            workers = []
+        # A single SGLang server exposes parser endpoints itself but not the
+        # router-only /workers endpoint. Use it as a final parser target so
+        # the native schema-aware parser remains available in that topology.
+        workers.append(SGLangWorkerInfo(url=self._router_url, is_healthy=True))
 
         headers = {}
         if routing_key:
@@ -617,9 +621,12 @@
             return None
 
         try:
-            workers = await self.list_workers()
+            workers = list(await self.list_workers())
         except Exception:
-            return None
+            workers = []
+        # See parse_function_call: direct SGLang deployments do not expose
+        # /workers but do expose /separate_reasoning.
+        workers.append(SGLangWorkerInfo(url=self._router_url, is_healthy=True))
 
         headers = {}
         if routing_key:

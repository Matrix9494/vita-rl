[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vita-rl"
version = "0.1.0"
description = "Experiment and method code for VitaBench post-training research"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Dongruo Zhou", email = "zhoudongruo@gmail.com" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
